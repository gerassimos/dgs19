public class BuildDependencies {

  public static void main(String[] args) {
    //NOTE
    // This is a dummy main class used at docker build time
    // just to download all the Dependencies (jars)
    // The goal is to have a Docker image with all the Dependencies downloaded in the local cache
  }
}
